{
	"name": "Google Apps Scripts Remove Warning",
	"version": "1.0",
	"description": "Webpages deployed via Google App Scripts now include an annoying message on top; this plugin removes that message for every page.",
	"content_scripts": [{
		"matches": ["*://script.google.com/*"],
		"js": ["src/main.js"],
		"run_at": "document_end"
	}],
	"manifest_version": 2
}
